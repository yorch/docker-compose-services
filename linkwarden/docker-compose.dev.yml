services:
  app:
    ports:
      - 3000:3000

  db:
    ports:
      - ${DEV_BIND_IP:-127.0.0.1}:5432:5432

  meilisearch:
    ports:
      - ${DEV_BIND_IP:-127.0.0.1}:7700:7700

  adminer:
    ports:
      - ${DEV_BIND_IP:-127.0.0.1}:8080:8080
